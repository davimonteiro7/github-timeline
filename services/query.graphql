query { 
  user(login:"davimonteiro7"){
    name
    repositories(first:100, orderBy:{field: CREATED_AT ,direction:ASC}){
      nodes{
        createdAt
        name
        description
      } 
    }
  }
}